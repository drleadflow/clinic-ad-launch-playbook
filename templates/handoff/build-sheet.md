# <Client> — <Offer> Meta Launch Build Sheet (<date>)
> Dummy-proof build sheet. If this page and Ads Manager disagree, this page wins until the owner says otherwise.
**Related:** Campaign Hub · Client Portal · Creative folder: <link> · Source repo: <path>

# 0. THE ONE THING THAT CANNOT BE WRONG: the landing page
```
<exact URL with utm_source, utm_medium, utm_campaign>
```
1. Website URL field: paste the full URL above, including everything after the `?`.
2. URL parameters field: `utm_content={{ad.name}}`.
3. Open Meta's preview link. It must show <offer name>, <price>, <provider name>. Anything else: stop.
4. The address bar on the preview still contains `utm_campaign=<value>`.
Before publishing: [ ] page loads under 3 s on mobile · [ ] one test lead lands in CRM location `<id>` and enters the right workflow · [ ] Pixel `<id>` fires Lead in Test Events.

# 1. Control panel
| Field | Value | Status |
|---|---|---|
| Client / address | | |
| Provider(s) | | |
| Medical director / allowed credential wording | | |
| Offer (one price, live) | | |
| Landing page | section 0 | |
| Ad account / Page / IG / Pixel / CRM location | | |
| Objective / geo / audience / budget | | |
| Launch decision | Do not spend until section 7 is cleared | owner |

# 2. Allowed on an ad / never on an ad
# 3. Campaign build
Campaign name · objective · budget type · settings on every ad (Standard Enhancements OFF, one text + one headline, URL exactly as section 0, `utm_content={{ad.name}}`) · ad naming.
| Ad set | Who it is for | Cells | CTA | Budget | Exclusions |
|---|---|---|---|---|---|
| A. Offer statics (prospecting) | solution + product aware | | Learn More | | |
| B. Awareness (no price) | unaware + problem aware | | Learn More | | |
| C. Retargeting | warm only | | Book Now | | |
| D. Controls | account's proven path | | | | |

# 4. Creative roster
Shared closing line(s). Then per cell: **ID · headline** (format) / Primary: … / Headline: … ★ = launch first.
# 5. Pre-flight checklist (builder signs each line; ends with "saved as paused")
# 6. Success and kill lines
# 7. Blockers before spend (client signs)
# 8. Compliance guardrails
# 9. Where everything lives
